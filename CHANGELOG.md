# Changelog

## 2.0.0

- **Konfigurierbarer PL/pgSQL-Formatierer:** Neuer Formatierer für SQL- und PL/pgSQL-Code, der über die VS-Code-Standardfunktion **„Format Document"** (`Shift+Alt+F`), den Befehl **„PostgreSQL: Format PL/pgSQL"** (Command Palette und Editor-Kontextmenü), einen **„Format PL/pgSQL"**-Button im SQL-Editor sowie optional beim Speichern erreichbar ist. Der Formatierer rückt PL/pgSQL-Blöcke (`DECLARE`/`BEGIN`/`END`, `IF`/`ELSIF`/`ELSE`, `LOOP`) ein, bricht SQL-Klauseln (`SELECT`/`FROM`/`WHERE`/`JOIN`/`ORDER BY` …) und Spaltenlisten strukturiert um, vereinheitlicht die Schreibweise von Schlüsselwörtern und Datentypen und behält dabei Zeichenketten, Kommentare und vom Benutzer gesetzte Leerzeilen unverändert bei. `CASE`-Ausdrücke werden korrekt als Ausdruck behandelt (kein fälschliches Aufbrechen an `ELSE`/`END`); Funktionsrümpfe in `$$ … $$` werden rekursiv formatiert. Die Ausgabe ist eine reine Neuformatierung der vorhandenen Token, sodass keine Bestandteile einer Anweisung verloren gehen, und sie ist idempotent (erneutes Formatieren ändert nichts mehr).
  - Konfigurierbar über folgende Einstellungen (Standardwerte in Klammern): `postgresQueryBuilder.format.enable` (an), `postgresQueryBuilder.format.formatOnSave` (aus), `postgresQueryBuilder.format.keywordCase` (`upper`), `postgresQueryBuilder.format.identifierCase` (`preserve`), `postgresQueryBuilder.format.dataTypeCase` (`upper`), `postgresQueryBuilder.format.indentStyle` (`space`), `postgresQueryBuilder.format.indentSize` (`2`) und `postgresQueryBuilder.format.commaStyle` (`trailing`). Der Formatierer ist für die Sprach-IDs `sql`, `postgres` und `pgsql` registriert.
  - **Verbesserungen am Formatierer:**
    - **Routinen-Header (`CREATE FUNCTION`/`PROCEDURE`):** Der Kopf wird jetzt strukturiert formatiert — Parameterlisten werden je nach Anzahl umgebrochen (siehe `argsInlineMax`/`argsMultilineMin`), `RETURNS …` bleibt auf der Zeile der schließenden Klammer, Charakteristika (`LANGUAGE`, `STABLE`, `SECURITY DEFINER`, `SET …` …) stehen je in einer eigenen, eingerückten Zeile, `AS` beginnt in Spalte 0, und der Rumpf (`DECLARE`/`BEGIN`/`END`) ist nicht zusätzlich eingerückt.
    - **`EXCEPTION … WHEN … THEN`-Blöcke** in PL/pgSQL werden jetzt korrekt umgebrochen und eingerückt (`EXCEPTION` auf `BEGIN`-Ebene, `WHEN` eine Ebene tiefer, der Handler-Rumpf nochmals eine Ebene tiefer).
    - **`IF [NOT] EXISTS`** (z. B. `CREATE SCHEMA IF NOT EXISTS …`, `DROP TABLE IF EXISTS …`) wird nicht mehr fälschlich als PL/pgSQL-`IF`-Block behandelt und umgebrochen.
    - **Typmodifizierer** wie `VARCHAR(500)` oder `NUMERIC(10,2)` behalten ihre Klammer ohne eingefügtes Leerzeichen.
    - **Klauselwörter innerhalb von Funktionsaufrufen** (z. B. `substring(… FROM 1 FOR 500)`) werden nicht mehr fälschlich als SQL-Klauseln umgebrochen; das Umbrechen von `SELECT`/`FROM`/`WHERE`/… greift nur noch im echten SQL-Kontext (Unterabfragen).
    - **`ARRAY[…]`-Literale** bleiben einzeilig (Kommas innerhalb der eckigen Klammern lösen keinen Listenumbruch aus).
    - **Argument-/Parameterumbruch konfigurierbar:** Aufruf- und Parameterlisten werden bei ≤ `argsInlineMax` Argumenten immer einzeilig gehalten und ab `argsMultilineMin` Argumenten immer mehrzeilig umgebrochen; dazwischen bleibt die im Quelltext gewählte Anordnung erhalten.
    - **Leerzeilen 1:1 beibehalten:** Vom Benutzer gesetzte Leerzeilen werden standardmäßig exakt beibehalten (`blankLines: preserve`); alternativ lassen sie sich auf maximal eine zusammenfassen (`collapse`).
    - **Einfache `SELECT`-Anweisungen** (eine Spalte, höchstens eine Tabelle, höchstens eine `WHERE`-Bedingung, kein `JOIN`/`GROUP`/`ORDER`) bleiben standardmäßig auf einer Zeile (`simpleSelectSingleLine`).
    - **Einzeilig geschriebene Funktionen/Prozeduren bleiben einzeilig:** Ist eine `CREATE [OR REPLACE] FUNCTION`/`PROCEDURE`-Anweisung im Quelltext vollständig auf einer einzigen Zeile geschrieben (inkl. Rumpf in `$$ … $$`), wird sie nicht mehr in den mehrzeiligen Routinen-Header aufgebrochen, sondern bleibt einzeilig (Schlüsselwörter/Datentypen werden weiterhin vereinheitlicht, der Rumpf bleibt unverändert). Beispiel: `CREATE FUNCTION pk.g_pair() RETURNS VARCHAR LANGUAGE SQL IMMUTABLE AS $$ SELECT 'X'; $$;` bleibt so erhalten. Über mehrere Zeilen geschriebene Routinen werden weiterhin strukturiert formatiert.
    - **`SELECT`-/`INTO`-Listen: ein Wert bleibt in der Zeile, mehrere werden umgebrochen:** Wird nur ein einzelner Ausdruck selektiert, steht er jetzt direkt hinter dem `SELECT`-Schlüsselwort (`SELECT 1`); bei mehreren Ausdrücken wird die Liste wie bisher auf mehrere Zeilen aufgeteilt. Dasselbe gilt für die `INTO`-Zielliste in `SELECT … INTO`. `INTO` (z. B. `INTO STRICT v_dummy`) beginnt nun grundsätzlich eine eigene Zeile (nur `INSERT INTO …` bleibt einzeilig).
    - **`AND`-Bedingungen je in eigener, eingerückter Zeile:** In `WHERE`-/`HAVING`-/`ON`-Bedingungen beginnt jedes top-level `AND` jetzt eine neue, um eine Ebene tiefer eingerückte Zeile. Ausnahme ist das zu einem `BETWEEN` gehörende `AND` — `… AND x BETWEEN y AND z` bleibt zusammen in einer Zeile.
    - **`%TYPE`/`%ROWTYPE` ohne Leerzeichen:** Attributtyp-Verweise wie `bos_mitarbeiter%ROWTYPE` oder `t.c%TYPE` werden jetzt ohne Leerzeichen um das `%` direkt an den Namen angehängt (`v_mitRec bos_mitarbeiter%ROWTYPE;`). Das Modulo-`%` als Operator (`a % b`) bleibt davon unberührt und behält seine Leerzeichen.
    - **Typnamen normalisieren:** Ausführliche, mehrteilige Typbezeichnungen werden beim Formatieren auf ihre Kurzform vereinheitlicht — `character varying` → `varchar`, `bit varying` → `varbit`, `timestamp without time zone` → `timestamp`, `timestamp with time zone` → `timestamptz`, `time without time zone` → `time`, `time with time zone` → `timetz` (Typmodifizierer wie `(500)` bleiben erhalten, z. B. `character varying(500)` → `varchar(500)`). Steuerbar über die neue Einstellung `postgresQueryBuilder.format.normalizeDataTypes` (Standard: an).
    - **Nachgestellte Zeilenkommentare bleiben in der Zeile:** Ein `--`-Kommentar, der im Quelltext am Ende einer Zeile steht (z. B. hinter dem abschließenden `;` einer einzeiligen Funktion), wird nicht mehr in eine eigene Zeile verschoben, sondern bleibt an dieser Zeile hängen. Kommentare, die bereits in einer eigenen Zeile stehen, behalten ihre eigene Zeile.
    - **Leerzeilen vor Anweisungen wie `CREATE`/`UPDATE`/`INSERT` bleiben erhalten:** Vom Benutzer gesetzte Leerzeilen vor einer Anweisung, die nicht mit einem Klausel-Schlüsselwort beginnt (z. B. mehrere leere Zeilen zwischen zwei `CREATE FUNCTION`-Definitionen), wurden bisher entfernt. Sie werden nun — wie bei den übrigen Klauseln — 1:1 beibehalten.
    - **Leerzeilen vor Kommentaren bleiben erhalten:** Eine vom Benutzer gesetzte Leerzeile direkt vor einem eigenständigen `--`-Kommentar (z. B. vor einer Abschnittsüberschrift im `DECLARE`-Block) wurde bisher entfernt; sie wird jetzt beibehalten.
    - **Präfix-Stringliterale wie `E'…'` bleiben unverändert:** Escape-/Bit-/Hex-/Unicode-Stringliterale (`E'\n'`, `B'1010'`, `X'1f'`, `U&'\0041'` sowie ihre Kleinschreibung) werden jetzt als ein Token erkannt. Bisher wurde der Präfix vom Stringinhalt getrennt (`E '\n'`), was zu fehlerhaftem SQL führte.
  - **Neue Einstellungen (Standardwerte in Klammern):** `postgresQueryBuilder.format.blankLines` (`preserve`), `postgresQueryBuilder.format.simpleSelectSingleLine` (`true`), `postgresQueryBuilder.format.argsInlineMax` (`1`), `postgresQueryBuilder.format.argsMultilineMin` (`4`) und `postgresQueryBuilder.format.normalizeDataTypes` (`true`).

## 1.3.0

- **„View Data": Spaltenüberschriften und Filterzeile bleiben beim Scrollen sichtbar (sticky):** Beim vertikalen Scrollen durch die Daten bleiben jetzt sowohl die Spaltenüberschriften als auch die darunterliegende Filterzeile fest am oberen Rand der Tabelle stehen. Da die Kopfzeile zweizeilig ist (Spaltenname + Datentyp) und ihre Höhe vom Inhalt abhängt, wird die tatsächliche Kopfzeilenhöhe gemessen und die Filterzeile exakt darunter angedockt (auch nach Größenänderung des Fensters neu berechnet). Eine mitscrollende Trennlinie sorgt dafür, dass die fixierten Zeilen klar vom Datenbereich abgegrenzt bleiben.

- **„View Data": Spalten werden sofort angezeigt, noch während die Daten laden:** Beim Öffnen einer Tabelle wird die Spaltenüberschrift (Tabellenkopf) jetzt angezeigt, sobald die Spalteninformationen vorliegen — also bereits, während die (unter Umständen langsame) `SELECT`- und `COUNT`-Abfrage noch läuft. Dadurch lässt sich z. B. der **„Constraints"**-Dialog schon öffnen und eine dauerhafte Bedingung hinzufügen, bevor die Daten vollständig geladen sind. Technisch sendet die Erweiterung nach dem Abrufen der Spalten umgehend eine separate `columnsLoaded`-Nachricht an die Ansicht (nur für die erste Seite einer regulären Tabellenansicht; „Load More" und Custom-Query-Ergebnisse bleiben unberührt); der Ladeindikator für die Daten bleibt sichtbar, bis die Zeilen eingetroffen sind.

- **„View Data": dauerhafte (permanente) Tabellen-Constraints:** Über den neuen Button **„Constraints"** in der Werkzeugleiste der Datenansicht lassen sich pro Tabelle feste `WHERE`-Bedingungen hinterlegen, die **automatisch bei jedem Öffnen dieser Tabelle** an die Standard-Abfrage angehängt werden (z. B. `CURRENT_TIMESTAMP BETWEEN valid_from AND valid_to`, um stets nur die aktuell gültigen Datensätze anzuzeigen). Der Bedingungs-Editor nutzt **dasselbe Design wie „+ Add fixed condition" im JOIN-Dialog**: Jede Bedingung besteht aus linkem Operand, Operator und rechtem Operand; Operanden werden entweder per Dropdown als **Spalte** der Tabelle oder als **freier Wert/Ausdruck** (z. B. `'TGB'` oder `CURRENT_TIMESTAMP`) eingegeben. Als Operatoren stehen `=`, `<>`, `<`, `<=`, `>`, `>=`, `LIKE`, `ILIKE`, `BETWEEN` (zwei rechte Operanden) sowie `IS NULL`/`IS NOT NULL` (ohne rechten Operanden) zur Verfügung. Eine Live-Vorschau zeigt die resultierende Abfrage; mit **„Save & Apply"** werden die Bedingungen dauerhaft gespeichert und die Ansicht sofort neu geladen. Die Bedingungen werden strukturiert im globalen Zustand der Erweiterung pro `schema.tabelle` abgelegt und gelten weiterhin als reguläre `WHERE`-Klausel, sodass die Datensätze in der Datenansicht bearbeitbar bleiben (Primärschlüssel-/Fremdschlüssel-Metadaten werden weiterhin geladen).
- **„View Data": `IS NULL` / `IS NOT NULL`-Filter per Spalten-Rechtsklick:** Im Kontextmenü einer Spaltenüberschrift in der Datenansicht gibt es jetzt die Einträge **„Add `<Spalte>` IS NULL to WHERE"** und **„Add `<Spalte>` IS NOT NULL to WHERE"**. Damit lässt sich mit einem Klick eine `IS NULL`- bzw. `IS NOT NULL`-Bedingung für die gewählte Spalte zur aktuellen `WHERE`-Klausel hinzufügen; eine bereits bestehende Bedingung auf derselben Spalte wird dabei ersetzt. Die geänderte Abfrage wird sofort ausgeführt.
- **Custom Mapping: Mehrspaltige Verknüpfung (zusammengesetzter Schlüssel):** Benutzerdefinierte Spalten-Verknüpfungen (Custom Mappings) können jetzt mehrere Spaltenpaare abgleichen, um zusammengesetzte Schlüssel (Composite Keys) abzubilden, z. B. `a.order_id = b.order_id AND a.tenant_id = b.tenant_id`. Im Mapping-Dialog gibt es dazu den neuen Abschnitt **„Additional column matches"** mit dem Button **„+ Add column match"**, der dieselbe Bedienlogik wie der Build-JOIN-Dialog verwendet: Quell-Spalte per Dropdown auswählen und Ziel-Spalte per Typeahead eingeben; alle Paare werden mit `AND` kombiniert. Beim Ableiten von JOINs erzeugt jedes Spaltenpaar eine eigene ON-Bedingung, sodass im JOIN-Dialog automatisch die vollständige zusammengesetzte Verknüpfung vorbelegt wird. Umgekehrte (reverse) Mappings tauschen Quell- und Zielspalte aller Paare korrekt. Bestehende einspaltige Mappings bleiben unverändert kompatibel.
- **„View Data": UPDATE/DELETE für Tabellen ohne Primärschlüssel:** Bei Tabellen ohne Primärschlüssel schlugen Änderungen und Löschungen in der Datenansicht bisher fehl bzw. erzeugten eine leere `WHERE`-Klausel. Jetzt wird die betroffene Zeile in diesem Fall über **alle Spaltenwerte** identifiziert (Vollzeilen-Abgleich), sodass `UPDATE`- und `DELETE`-Anweisungen die richtige Zeile treffen. `NULL`-Werte in der Identifizierung werden korrekt als `IS NULL` (statt `= NULL`) abgebildet — sowohl in der SQL-Vorschau als auch beim parametrisierten Commit. Zur Sicherheit wird eine Anweisung mit komplett leerer `WHERE`-Klausel abgelehnt (mit Rollback der Transaktion), damit niemals versehentlich alle Zeilen geändert oder gelöscht werden. Tabellen mit Primärschlüssel verhalten sich unverändert (Identifizierung weiterhin nur über die PK-Spalten).
- **JOIN-Dialog: dieselbe Tabelle mehrfach hinzufügbar (Self-Join):** Im JOIN-Dialog kann jetzt dieselbe Tabelle mehrmals mit jeweils eigenem Alias und eigener Join-Bedingung hinzugefügt werden (z. B. für Self-Joins wie `employees e LEFT JOIN employees m ON m.id = e.mgr_id`). Beim Hinzufügen über „+ Add tables…" werden bereits im Join enthaltene Tabellen nicht mehr ausgefiltert, sondern in der Auswahlliste mit dem Hinweis „already in join — adds another instance" angezeigt. Jede hinzugefügte Instanz erhält einen eindeutigen Alias und wird als eigenständige Tabelle behandelt; standardmäßig wird die zusätzliche Instanz als `CROSS JOIN` eingefügt, sodass die Join-Bedingung anschließend frei gegen eine beliebige frühere Tabelle (auch eine andere Instanz derselben Tabelle) gesetzt werden kann.
- **JOIN-Dialog: Auto-Vorbelegung der ON-Bedingungen auch beim erneuten/mehrfachen Hinzufügen:** Eine erneut oder mehrfach hinzugefügte Tabelle erhält jetzt dieselben automatisch abgeleiteten Join-Bedingungen wie beim ersten Hinzufügen. Die Zuordnung erfolgt nun durchgängig über die Tabellen-/Spaltennamen aus den Fremdschlüssel- und Custom-Mapping-Kanten (statt über einen einzelnen Tabellenindex), sodass auch eine zweite Instanz derselben Tabelle korrekt mit der passenden früheren Tabelle (inkl. Self-Join über selbstreferenzierende Fremdschlüssel) verbunden wird.
- **JOIN-Dialog: Alias-Nummerierung gibt freigewordene Suffixe wieder frei:** Wird eine zuvor hinzugefügte Tabelle (z. B. mit Alias `tabelle2`) wieder entfernt und erneut hinzugefügt, erhält sie wieder `tabelle2` statt fortlaufend `tabelle3`. Die Eindeutigkeitsprüfung berücksichtigt jetzt nur noch die Aliase der aktuell sichtbaren Tabellen, sodass durch Entfernen freigewordene Nummern erneut vergeben werden.
- **JOIN-Dialog: feste (fixe) Zusatzbedingungen in der ON-Klausel:** Über den neuen Button **„+ Add fixed condition"** lassen sich pro Join beliebige feste Bedingungen zur ON-Klausel hinzufügen, z. B. `CURRENT_TIMESTAMP BETWEEN t.valid_from AND t.valid_to` oder `t.type = 'TGB'`. Jede Bedingung wird komfortabel aus drei Teilen zusammengesetzt: linker Operand, Operator und rechter Operand. Operanden lassen sich entweder per Dropdown als **Spalte** (qualifiziert über den Tabellen-Alias) auswählen oder als **freier Wert/Ausdruck** eingeben (z. B. ein String-Literal `'TGB'` oder ein SQL-Ausdruck wie `CURRENT_TIMESTAMP`). Als Operatoren stehen `=`, `<>`, `<`, `<=`, `>`, `>=`, `LIKE`, `ILIKE`, `BETWEEN` (zwei rechte Operanden, mit `AND` verbunden) sowie `IS NULL`/`IS NOT NULL` (ohne rechten Operanden) zur Verfügung. Die festen Bedingungen werden in der Vorschau und im erzeugten Statement an die übrigen ON-Bedingungen mit `AND` angehängt und bleiben beim Umsortieren der Tabellen erhalten.
- **JOIN-Dialog: automatisch eingefügte Custom-Mapping-Bedingungen bearbeitbar:** Die aus benutzerdefinierten Spalten-Verknüpfungen (Custom Mappings) automatisch vorbelegten Zusatzbedingungen, die bisher nur schreibgeschützt angezeigt wurden, lassen sich jetzt über einen **„Edit"**-Button in eine editierbare feste Bedingung umwandeln. Dabei wird die Bedingung in den neuen Bedingungs-Editor (linker Operand / Operator / rechter Operand) überführt: Die Spalte bleibt als alias-qualifizierte Spaltenauswahl erhalten, der Wert wird als frei editierbares Literal übernommen. Anschließend können Operator und Operanden beliebig angepasst werden; die ursprüngliche schreibgeschützte Zeile wird dabei entfernt, sodass die Bedingung nicht doppelt erscheint.

## 1.2.0

- **„View Data": Variablen nach `LIMIT`/`OFFSET` und PL/pgSQL-Record-Felder werden erkannt:** Der Variablen-Dialog erkennt jetzt auch Variablen, die als Zeilenanzahl hinter `LIMIT` bzw. `OFFSET` stehen (z. B. `LIMIT pi_max_history` → `pi_max_history` wird abgefragt). Zusätzlich werden Feldzugriffe auf PL/pgSQL-Record-Variablen (z. B. `pi_employeeObj.emplId` in `… = pi_employeeObj.emplId`) als eine einzige Variable behandelt und ersetzt. Um sie von echten, tabellen-qualifizierten Spalten (`lbe.lbe_mit_id`) zu unterscheiden, werden alle Tabellennamen und Aliase aus den `FROM`/`JOIN`-Klauseln (auch in Subqueries) gesammelt: Ist der Bezeichner vor dem Punkt **kein** bekannter Tabellen-/Alias-Name (und liegt kein Funktionsaufruf `x.y(…)` bzw. keine längere Kette `x.y.z` vor), gilt der punktierte Bezeichner als Record-Variable. Numerische `LIMIT`-Literale (`LIMIT 100`) und korrelierte Verweise auf einen äußeren Tabellen-Alias bleiben unangetastet.
- **`search_path` anzeigen und ändern:** In der Titelleiste der Tabellen-Baumansicht gibt es jetzt einen Button **„Show / Edit search_path"** (auch über die Befehlspalette als *PostgreSQL: Show / Edit search_path* erreichbar). Er zeigt den aktuell wirksamen `search_path` der aktiven Verbindung in einem Eingabefeld an und erlaubt, diesen zu ändern (kommagetrennte Schema-Liste). Ein leeres Feld stellt den Server-Standard wieder her. Der neue Pfad wird über `set_config('search_path', …)` (als Parameter, also injektionssicher) auf alle Verbindungen des Pools angewendet — der Pool wird dafür neu aufgebaut und der Metadaten-Cache geleert. Anschließend wird die Baumansicht aktualisiert, sodass die geänderte Schema-Auflösung (Qualifizierung von Tabellen) sofort greift.
- **Gruppierung der Relationen nach Typ in der Baumansicht:** Unter jedem Schema werden die Relationen jetzt in eigenen, automatisch aufgeklappten Unterknoten gruppiert: **Tables**, **Views**, **Materialized Views** und **Foreign Tables** (in dieser Reihenfolge). Es werden nur Kategorien angezeigt, die tatsächlich Relationen enthalten; jede Kategorie und jedes Schema sind standardmäßig expandiert. Die Relations-Icons unterscheiden sich je nach Typ. Bei aktivem Suchfilter bleiben nur die Kategorien sichtbar, die mindestens eine passende Relation enthalten.
- **Views in der Tabellen-Baumansicht sichtbar:** (Regel-)Views werden jetzt zusätzlich zu Tabellen und Foreign Tables in der Baumansicht, der Tabellen-Suche, dem JOIN-Dialog und der Typeahead-Auswahl angezeigt. Die Auflistung berücksichtigt dazu den `table_type` `'VIEW'` aus `information_schema.tables`. Auch **materialisierte Views** erscheinen jetzt in denselben Ansichten: Da PostgreSQL diese nicht über `information_schema` ausgibt, werden sie zusätzlich aus `pg_matviews` ermittelt und per `UNION` zusammengeführt.
- **`search_path`-Erkennung beim Öffnen von Tabellen korrigiert:** Ob eine Tabelle mit Schema qualifiziert werden muss, wird jetzt zuverlässig aus `current_setting('search_path')` (entspricht `SHOW search_path`) ermittelt — statt aus `current_schemas(false)`. Letzteres spiegelt nur den real aufgelösten Pfad der gerade ausführenden Session wider und lieferte auf gepoolten Verbindungen teils eine **leere Liste**, obwohl das Schema (z. B. `leda`) klar im `search_path` lag. Dadurch wurden Tabellen unnötig qualifiziert (`SELECT * FROM leda.tabelle` statt `SELECT * FROM tabelle`). Der `search_path`-String wird nun selbst geparst (Komma-Trennung, doppelt gequotete Bezeichner inkl. `\"\"`-Escaping, sowie die Auflösung des Sonder­eintrags `$user`/`\"$user\"` auf den aktuellen Benutzer). Das Diagnose-Log zeigt jetzt den rohen `search_path`-String und den aufgelösten Benutzer.
- **Diagnose-Ausgabe im „PostgreSQL Query Builder"-Output-Kanal:** Wichtige Vorgänge werden jetzt in den VS-Code-Output-Kanal **„PostgreSQL Query Builder"** protokolliert (zeitgestempelt und nach Bereich gruppiert). Geloggt werden u. a. der beim Verbinden ermittelte Server-`search_path` (`SHOW search_path`), die Verbindungsdaten (Host/aufgelöste IP, Port, Datenbank, Benutzer, Anzeige-Schemas), sowie pro geöffneter Tabelle der zur Laufzeit abgefragte `search_path` (`current_schemas(false)`) und die daraus abgeleitete Tabellen-Referenz inkl. Begründung (ob/warum mit Schema qualifiziert wird). Das erleichtert die Diagnose von Fällen, in denen eine Tabelle aus dem falschen Schema geöffnet wird.
- **Schema-Qualifizierung beim Öffnen von Tabellen außerhalb des `search_path`:** Beim Öffnen einer Tabelle wird der erzeugte `SELECT` jetzt korrekt mit dem Schema qualifiziert (`SELECT * FROM leda_dev.tabelle` statt `SELECT * FROM tabelle`), wenn das Schema zwar in der Verbindung zur Anzeige im Baum konfiguriert ist, aber nicht im tatsächlichen PostgreSQL-`search_path` liegt. Bisher wurde die in der Verbindung hinterlegte Schema-Liste (ein reiner Anzeige-Filter) fälschlich als `search_path` interpretiert. Dadurch wurde z. B. eine Foreign Table aus `leda_dev` ohne Schema-Präfix abgefragt und die gleichnamige lokale Tabelle aus dem `search_path`-Schema (`leda`) geöffnet. Maßgeblich ist jetzt ausschließlich der zur Laufzeit ermittelte `search_path` (`current_schemas(false)`).
- **Foreign Tables erscheinen jetzt in der Tabellen-Baumansicht:** Fremd-Tabellen (Foreign Tables, z. B. über `postgres_fdw` eingebundene Tabellen) wurden in der Baumansicht, der Tabellen-Suche, dem JOIN-Dialog und der Typeahead-Auswahl nicht angezeigt, wenn der PostgreSQL-Server sie in `information_schema.tables` als `'FOREIGN TABLE'` meldet (Server-Versionen < 9.5). Die Filter berücksichtigen jetzt beide Schreibweisen (`'FOREIGN'` und `'FOREIGN TABLE'`) über eine gemeinsame Konstante, sodass Foreign Tables versionsunabhängig wie normale Tabellen aufgelistet werden.
- **Verbindung bearbeiten ohne Passwort-Eingabe:** Beim Bearbeiten einer bestehenden Verbindung (z. B. um nur den Port, Host oder die Datenbank anzupassen) muss das Passwortfeld nicht mehr erneut ausgefüllt werden. Bleibt das Feld leer, wird automatisch das zuvor gespeicherte Passwort dieser Verbindung weiterverwendet, statt es mit einem leeren Wert zu überschreiben. Wird ein neues Passwort eingegeben, ersetzt dieses wie gewohnt das alte. Bei neuen Verbindungen bleibt das Verhalten unverändert.

## 1.1.1

- **„View Data" innerhalb einer `FOR … IN (…) LOOP`-Schleife:** Bei einem Rechtsklick auf eine `SELECT`-Anweisung, die als Schleifen-Query in einem PL/pgSQL-`FOR r IN ( SELECT … ) LOOP`-Konstrukt steht, wird jetzt nur noch die eigentliche `SELECT`/`WITH`-Abfrage extrahiert. Das umgebende Schleifengerüst (`FOR r IN (`, das schließende `) LOOP …`) sowie der Schleifenkörper werden nicht mehr mit übernommen. Sowohl die geklammerte Variante (`FOR r IN ( SELECT … ) LOOP`, auch mit `UNION ALL`, verschachtelten Klammern/Subqueries und einem führenden Kommentar vor dem `SELECT`) als auch die ungeklammerte Variante (`FOR r IN SELECT … LOOP`) werden korrekt erkannt.
- **Variablen-Erkennung in Funktionsargumenten:** Variablen, die als Argument eines Funktionsaufrufs in einer Wert-Position stehen (z. B. `v_von` in `DATE_TRUNC('day', v_von)` oder `v_bis` in `LAST_DAY(v_bis)` bzw. `LEAST(v_bis, …)`), werden im Variablen-Dialog jetzt ebenfalls erkannt und ersetzt. Der Wert-Kontext wird in die Argumentliste eines Funktionsaufrufs weitergereicht (auch verschachtelt), während Funktionsargumente außerhalb einer Wert-Position (z. B. in der SELECT-Liste) weiterhin nicht als Variablen behandelt werden.
- **„View Data" für `UPDATE`-Anweisungen:** Ein Rechtsklick mit **„View Data"** auf eine `UPDATE`-Anweisung wandelt diese jetzt automatisch in ein gleichwertiges `SELECT * FROM …`-Statement um, damit die betroffenen Zeilen angezeigt werden können. Die `SET`-Zuweisungsliste und eine eventuelle `RETURNING`-Klausel werden entfernt, die `WHERE`-Bedingung (inkl. verschachtelter Subqueries und `BETWEEN`) bleibt erhalten. Eine optionale `UPDATE … FROM <Liste>`-Klausel wird in die `FROM`-Liste des SELECTs übernommen, Tabellen-Aliase bleiben bestehen. Variablen in der `WHERE`-Bedingung werden wie gewohnt über den Variablen-Dialog abgefragt.
- **Spalten werden im Variablen-Dialog nicht mehr als Variablen vorgeschlagen:** Beim „View Data"-Befehl werden die im Statement referenzierten Tabellen (alle Tabellen nach `FROM`/`JOIN`, auch in Subqueries und schema-qualifiziert) ermittelt und ihre echten Spaltennamen aus `information_schema.columns` abgefragt. Bezeichner, die einem dieser Spaltennamen entsprechen (z. B. `sed_persnr`, `sed_counter`, `mtd_von`, `mtd_datum`, `mtd_bis` aus Spalte-gegen-Spalte-Vergleichen oder `COALESCE(…)`-Argumenten), werden aus der Liste der abzufragenden Variablen entfernt — nur tatsächliche PL/pgSQL-Variablen und Positionsparameter bleiben übrig. Schlägt die Spaltenabfrage fehl oder besteht keine Verbindung, wird wie bisher die ungefilterte Liste verwendet.

## 1.1.0

- Die **Datenansicht** öffnet jetzt nur noch beim **Doppelklick** auf eine Tabelle in der Baumansicht (statt beim einfachen Klick), damit das Navigieren/Auswählen in der Tabellenliste nicht mehr versehentlich Tabellen öffnet.
- Die Checkbox **„Set as default (show FK button in cell)"** im Mapping-Dialog der Tabellenansicht ist bei neuen Mappings jetzt standardmäßig aktiviert, sodass der FK-Sprungknopf direkt in der Zelle angezeigt wird.
- Im **JOIN-Dialog** lässt sich die Reihenfolge der Tabellen jetzt zusätzlich per **Drag & Drop** ändern (neben den Pfeil-Buttons). Eine Tabelle wird am Ziehgriff (☰) gegriffen und an die gewünschte Position gezogen; die bereits konfigurierten Join-Bedingungen bleiben dabei erhalten — auch wenn eine Tabelle an die erste Position (FROM) verschoben wird, wird die Join-Bedingung automatisch der jeweils späteren Tabelle zugeordnet und geht nicht mehr verloren.
- Im **JOIN-Dialog** lassen sich jetzt **weitere Tabellen hinzufügen**: Ein Klick auf „+ Add tables…" öffnet eine Mehrfachauswahl aller verfügbaren Tabellen (hat man zuvor Tabellen in der Baumansicht markiert, werden diese direkt übernommen). Die gewählten Tabellen werden ans Ende der Tabellenliste angehängt. Join-Bedingungen werden für die neuen Tabellen automatisch aus vorhandenen Fremdschlüsseln zu den bereits vorhandenen Tabellen vorbelegt; bereits konfigurierte Reihenfolge, Aliase und Bedingungen der vorhandenen Tabellen bleiben unverändert. Bereits enthaltene Tabellen werden übersprungen, Aliase bei Bedarf eindeutig gemacht.
- Im **JOIN-Dialog** lassen sich Tabellen jetzt auch wieder **entfernen**: Jede Tabellenzeile hat einen ✕-Button. Beim Entfernen werden die zugehörige Join-Klausel sowie alle Bedingungen anderer Tabellen, die auf die entfernte Tabelle verweisen, mit aufgeräumt; die letzte verbleibende Tabelle lässt sich nicht entfernen. Eine zuvor entfernte Tabelle kann über „+ Add tables…" wieder hinzugefügt werden.
- Im **JOIN-Dialog** werden die Join-Bedingungen jetzt zusätzlich aus **benutzerdefinierten Spalten-Verknüpfungen (Custom Mappings)** vorbelegt — nicht mehr nur aus Datenbank-Fremdschlüsseln. Existiert zwischen zwei am Join beteiligten Tabellen ein Custom Mapping `A.spalte → B.spalte`, wird daraus automatisch eine Join-Bedingung abgeleitet (für die anfänglichen Tabellen wie auch für nachträglich hinzugefügte). Doppelte Bedingungen (bereits über einen Fremdschlüssel abgedeckt) werden vermieden.
- Im **JOIN-Dialog** werden jetzt auch die im Custom Mapping hinterlegten **Zusatzbedingungen** in die ON-Klausel übernommen. Hat ein Mapping neben der Spalten-Verknüpfung weitere Bedingungen (z. B. `status = 'active'`), werden diese als literale Bedingungen an die jeweilige Join-Bedingung angehängt (z. B. `... ON b.id = a.b_id AND a.status = 'active'`). Die Werte werden korrekt als Zahl bzw. als gequotetes String-Literal formatiert. Die abgeleiteten Zusatzbedingungen werden im Dialog angezeigt, bleiben beim Umsortieren erhalten und lassen sich bei Bedarf einzeln entfernen.
- Der **JOIN-Dialog** lässt sich jetzt auch beim Ziehen einer **einzelnen Tabelle** öffnen: Wählt man beim Ablegen die Option **„Join"**, öffnet sich der interaktive JOIN-Builder mit dieser Tabelle als FROM-Tabelle. Über „+ Add tables…" lassen sich anschließend weitere Tabellen hinzufügen und die Join-Bedingungen konfigurieren (zuvor wurde nur ein statischer `JOIN … ON …`-Schnipsel eingefügt).
- **JSONB-/JSON-Werte sind in der Datenansicht jetzt sichtbar:** Spalten vom Typ `json`/`jsonb` werden vom Postgres-Treiber als JavaScript-Objekte/Arrays geliefert und wurden bisher als `[object Object]` angezeigt. Sie werden nun als formatierter JSON-Text dargestellt — sowohl in der Tabellenzelle als auch im Datensatz-Dialog („View full record") und in duplizierten Zeilen. Die Anzeige ist konsistent mit der Änderungserkennung, sodass unveränderte JSON-Zellen nicht mehr fälschlich als bearbeitet markiert werden.
- **„View Data" für SELECTs in Prozeduren/Funktionen:** In einer SQL-Datei (z. B. einem Schema-Skript mit vielen Prozeduren und Funktionen) gibt es jetzt im Editor-Kontextmenü den Eintrag **„View Data"**. Beim Rechtsklick innerhalb einer `SELECT`-Anweisung werden deren Anfang und Ende automatisch erkannt (per Semikolon-Grenzen, wobei Kommentare sowie String- und Dollar-Quoted-Literale ignoriert werden); ein eventuell vorhandener Prefix wie `RETURN QUERY` wird auf das `SELECT`/`WITH` zurückgeschnitten. Hat man stattdessen einen **Sub-Select oder eine WITH-Klausel markiert**, wird nur die Markierung verwendet und nicht auf die gesamte Anweisung erweitert. Die `INTO`-Klausel (auch `INTO STRICT`) wird automatisch entfernt, damit die Anweisung eigenständig lauffähig ist.
- **Variablen-Abfrage per Modal mit Cache:** Werden in dem erkannten SELECT Variablen verwendet (Werte in Vergleichen/`IN`/`LIKE`/`BETWEEN`-Listen, Positionsparameter `$1` sowie `user`/`current_user`/`session_user`/`current_role`/`current_catalog`), öffnet sich ein **modaler Dialog**, in dem für jede Variable optional ein Wert angegeben werden kann. Leere Felder lassen den Bezeichner unverändert; Werte werden 1:1 eingesetzt (String-Literale also inklusive Anführungszeichen). Die zuletzt verwendeten Werte werden **pro Variablenname zwischengespeichert** und beim nächsten Mal vorausgefüllt. Anschließend wird das Ergebnis in der **bestehenden Datenansicht in einem geteilten Editor (Split-View)** im schreibgeschützten Modus angezeigt.
- **Kopieren von Zahlen ohne Tausendertrennzeichen:** Beim Kopieren aus der Datenansicht werden bei numerischen Spalten die Tausendertrennzeichen (standardmäßig Leerzeichen) entfernt — aus angezeigt `9 999 999,99` wird in der Zwischenablage `9999999,99`. Das Dezimaltrennzeichen bleibt unverändert. Bei Mehrfachauswahl über mehrere Zellen werden die Zellen tab-/zeilengetrennt kopiert, wobei nur numerische Spalten normalisiert und Textspalten unverändert übernommen werden; Auswahlen ohne numerische Zellen verhalten sich wie das normale Kopieren.
- **Mehrzeiliger, formatierter SELECT in der Datenansicht:** Die Abfragezeile der Datenansicht ist jetzt ein **mehrzeiliges Eingabefeld** und zeigt den SELECT **standardmäßig formatiert** an: Jede Top-Level-Spalte steht eingerückt in einer eigenen Zeile, die Klauseln `FROM`/`JOIN`/`WHERE`/`GROUP BY`/`ORDER BY`/`LIMIT` usw. beginnen jeweils auf einer neuen Zeile und `WHERE`-Bedingungen werden an Top-Level-`AND` umgebrochen. Die Formatierung greift automatisch beim Öffnen der Tabelle, beim Auswählen aus dem Abfrageverlauf sowie bei Filter-, Ausschluss- und Sortier-Aktionen; ein neuer **„Format"**-Button formatiert auch manuell eingegebenes SQL. Innerhalb des Felds fügt **Enter** einen Zeilenumbruch ein, **Strg/Cmd+Enter** führt die Abfrage aus. Zeichenketten, Klammern, Funktionsaufrufe und Kommentare werden dabei korrekt behandelt; Anweisungen mit Kommentaren, CTEs (`WITH`) oder Nicht-`SELECT`-Statements bleiben unverändert.


## 1.0.0

- **Tabelle per Drag & Drop in den Editor ziehen:** Eine Tabelle aus der Tabellen-Baumansicht lässt sich jetzt direkt in einen beliebigen Texteditor ziehen. Beim Ablegen erscheint eine Auswahl mit **Name only**, **Insert**, **Delete**, **Update**, **Select** (Standard) und **Join**; während des Navigierens wird eine Live-Vorschau des erzeugten Statements angezeigt. Nach Bestätigung wird das Statement an der Cursor-Position eingefügt. Alle Statements enthalten sämtliche Spalten der Tabelle und qualifizieren die Spalten mit einem Tabellen-Alias (z. B. `SELECT lei.lei_id ... FROM leistungen lei`). Der Alias wird aus dem Teil des ersten Spaltennamens vor dem ersten Unterstrich abgeleitet (`lei_id` → `lei`) bzw. fällt auf den Tabellennamen zurück. Über einen Stift-Button in der Auswahl lässt sich der Alias pro Tabelle ändern; die Wahl wird gespeichert und beim nächsten Mal wiederverwendet.
- **Formatierter SELECT:** Der generierte SELECT listet jede Spalte eingerückt in einer eigenen Zeile auf (`SELECT`-Schlüsselwort und `FROM`-Klausel auf separaten Zeilen).
- **DELETE mit allen Spalten in der WHERE-Klausel:** Das per Drag & Drop erzeugte DELETE-Statement listet alle Spalten der Tabelle (qualifiziert) in der WHERE-Klausel auf, jeweils mit `AND` verknüpft.
- **UPDATE mit Typ-Cast:** Das per Drag & Drop erzeugte UPDATE-Statement setzt alle Spalten und castet jeden Wert auf den jeweiligen Spaltentyp (z. B. `amount = NULL::numeric`).
- **INSERT mit Typ-Cast:** Das per Drag & Drop erzeugte INSERT-Statement castet ebenfalls jeden Wert auf den jeweiligen Spaltentyp (z. B. `NULL::numeric -- amount`).
- **Mehrere Tabellen als JOIN SELECT einfügen:** Mehrere Tabellen lassen sich (mit `Strg`/`Shift`-Mehrfachauswahl) gemeinsam aus der Baumansicht in einen Editor ziehen. Es öffnet sich ein Bestätigungsdialog, der ein `SELECT … FROM … JOIN …` über alle Tabellen mit Live-Vorschau anzeigt. Die Join-Bedingungen werden automatisch aus vorhandenen Primär-/Fremdschlüsseln zwischen den Tabellen vorbelegt (Tabellen ohne Beziehung fallen auf `CROSS JOIN` zurück). Im Dialog lassen sich die Reihenfolge der Tabellen ändern, Aliase umbenennen, der Join-Typ (INNER/LEFT/RIGHT/FULL/CROSS) wählen sowie Join-Bedingungen hinzufügen, entfernen und bearbeiten. Nach Bestätigung wird das fertige Statement an der Cursor-Position eingefügt.
## 0.2.1

- `TIMESTAMP WITHOUT TIME ZONE`-Werte werden jetzt 1:1 so angezeigt, wie sie in der Datenbank gespeichert sind. Bisher wurden diese Werte vom Postgres-Treiber als lokale Zeit interpretiert und beim Anzeigen in die Browser-Zeitzone (UTC) umgerechnet, sodass der dargestellte Zeitpunkt vom gespeicherten Wert abweichen konnte. Die Werte werden nun unverändert als Zeichenkette übernommen.
- Die Tabellensuche unterstützt jetzt mehrere durch Leerzeichen getrennte Suchbegriffe. Jeder Begriff wird einzeln gegen den qualifizierten Namen `schema.table` geprüft; Tabellen und Schemata mit den meisten Treffern werden oben in der Liste angezeigt.
- Treffer in der Tabellenliste werden zusätzlich farblich hervorgehoben (über die Standard-Trefferfarbe der VS Code-TreeView, theme-abhängig — anpassbar über `workbench.colorCustomizations` → `list.highlightForeground`).

## 0.2.0

- Lange Kontextmenüs in der Tabellenansicht sind jetzt scrollbar und werden innerhalb des Viewports positioniert (öffnen bei Bedarf nach oben).
- Datenansicht zeigt jetzt eine Zeilennummernspalte (`#`) vor den Aktionen an.
- Query-History ist jetzt ein durchsuchbares Typeahead-Panel (breiter, scrollbar, mit Tastatur-Navigation und Treffer-Hervorhebung) statt eines schmalen Dropdowns.
- Rechtsklick auf einen Spaltenkopf öffnet ein Kontextmenü, mit dem sich der `ORDER BY`-Teil der aktuellen Query setzen, erweitern oder entfernen lässt (ASC/DESC, Replace/Add). Ist die Spalte bereits Teil der `ORDER BY`-Klausel, gibt es zusätzlich „Remove `col` from ORDER BY".
- Spaltenfilter (Enter) und „Add as Exact Match to Query" überschreiben nicht mehr die eigene SELECT-Anweisung: bestehende `WHERE`-Bedingungen bleiben erhalten, nur eine vorhandene Bedingung für dieselbe Spalte wird ersetzt; andere Bedingungen und `ORDER BY` bleiben unverändert.
- Beim Öffnen einer verknüpften Tabelle (FK/PK/Custom Mapping) und bei „Add as Exact Match to Query" wird der Wert nur noch in die SELECT-Klausel gemergt — die Filterzeile bleibt leer (statt automatisch mit dem Wert vorbefüllt zu werden).
- Neues Panel **Modify History** unter dem Search-Panel: zeigt persistent (bis 500 Einträge) alle ausgeführten ändernden Statements (INSERT/UPDATE/DELETE/MERGE/TRUNCATE/CREATE/DROP/ALTER/GRANT/REVOKE/COMMENT/REFRESH/CALL) aus Tabellen-Commits, der Query-Bar der Datenansicht und dem SQL-Editor. Klick auf einen Eintrag kopiert das SQL, „Clear" leert die Historie.
- Tabellenansicht zeigt in der Toolbar die Verbindung an, mit der die aktuell sichtbaren Daten geladen wurden (wird bei jedem SELECT-Lauf aktualisiert). Wechselt die aktive Verbindung danach, wird die Anzeige als Warnung markiert.
- Der „SQL Preview / Execute"-Dialog der Tabellenansicht zeigt jetzt die aktuelle Verbindung und die Verbindung, mit der die Daten geladen wurden. Unterscheiden sich beide, erscheint ein deutlich sichtbarer Warnhinweis, dass die Ausführung gegen die aktive Verbindung läuft.
- Benutzerdefinierte Spalten-Verknüpfungen (Custom Mappings) werden jetzt in beide Richtungen angezeigt: ein Mapping `A.col → B.col` erscheint auch in der Tabelle `B` als „Jump to A.col (reverse)" im Zellen-Kontextmenü sowie als nur-lesbarer Eintrag mit Badge „Reverse" im Manage-Dialog. Bearbeitet/gelöscht werden Mappings weiterhin nur auf der ursprünglichen Quell-Tabelle.
- Custom Mappings lassen sich jetzt **über git mit Kollegen teilen**: jedes Mapping hat einen Scope „Personal" (wie bisher, lokal pro Benutzer in VS Code globalState) oder „Workspace" (gespeichert in `.vscode/postgres-query-builder.mappings.json` im Workspace und damit committable). Im Mapping-Dialog gibt es eine Checkbox „Share with workspace (commit to git)"; der Manage-Dialog zeigt Badges „Workspace"/„Personal". Der Pfad zur Workspace-Datei ist über die Einstellung `postgresQueryBuilder.customMappingsFile` konfigurierbar; Änderungen an der Datei (z. B. nach `git pull`) werden via FileSystemWatcher erkannt und alle offenen Tabellenansichten aktualisiert. Zusätzlich gibt es die Befehle „PostgreSQL: Export/Import Custom Column Mappings..." und „PostgreSQL: Open Workspace Custom Mappings File" für ad-hoc Austausch.
- Neuer Button **„Manage All Mappings..."** im Search-Panel (sowie Befehl „PostgreSQL: Manage Custom Column Mappings...") öffnet einen zentralen Verwaltungs-Tab über _alle_ Mappings mit Filter, Mehrfachauswahl und Bulk-Aktionen: ausgewählte Mappings per Klick auf „Move to Workspace (share)" gemeinsam teilen, mit „Move to Personal" wieder privat machen, oder mehrere auf einmal löschen. Quick-Selects für alle Personal- bzw. Workspace-Einträge sowie ein Link zum Öffnen der Workspace-Datei sind ebenfalls vorhanden.
- Im Verwaltungs-Tab „Manage All Mappings..." lassen sich einzelne Mappings nun auch direkt bearbeiten: ein „Edit"-Button je Zeile öffnet einen Dialog für Source-/Target-Spalten, Label, Default-Flag, Workspace-Sharing und Bedingungen — Änderungen werden sofort im jeweiligen Store gespeichert und in allen offenen Tabellenansichten aktualisiert.
- **„Load More" funktioniert jetzt auch für eigene Queries** aus der Query-Bar (z. B. nach Filtern oder Custom SELECTs): die SQL wird nicht mehr stumpf um `LIMIT 50 OFFSET 0` ergänzt und anschließend deaktiviert, sondern ein evtl. vorhandenes trailing `LIMIT/OFFSET` wird (paren-/string-/kommentar-sicher) abgetrennt, die Basis-SQL gemerkt, und „Load More" hängt weitere Seiten mit steigendem `OFFSET` an die bereits geladenen Zeilen an. Hat der Nutzer selbst ein explizites `LIMIT`/`OFFSET` geschrieben, bleibt der Button (wie bisher) deaktiviert.
- **NULL-Zellen sind jetzt direkt editierbar:** Ein Klick in eine als `NULL` angezeigte Zelle leert den Platzhalter sofort, sodass man losschreiben kann. Verlässt man die Zelle wieder leer, wird der Wert (wie bisher) als `NULL` gespeichert und der `NULL`-Platzhalter wird wieder angezeigt.
- **Breiteres Spalten-Feld bei Mapping-Bedingungen:** Im Mapping-Dialog (Tabellenansicht) und im „Manage All Mappings"-Panel ist die Spaltenauswahl für Bedingungen jetzt deutlich breiter (statt fixer 60 px), sodass auch lange Spaltennamen vollständig lesbar sind. Der Operator-Selektor bleibt schmal.
- **Single Record View:** Neue Detailansicht für einzelne Datensätze. Aufruf entweder über das neue Augen-Symbol (👁) in der Aktions-Spalte jeder Zeile oder über den Kontextmenü-Eintrag „View Full Record..." auf einer Zelle. Die Ansicht zeigt alle Spalten der Zeile zeilenweise an (Spaltenname mit PK/FK-Badge und Typ links, vergrößerbares Textarea-Feld für den Wert rechts, optional ein FK-Sprungknopf rechts daneben), unterstützt direktes Editieren (leer = `NULL`, numerische Spalten werden normalisiert), zeigt geänderte Felder hervorgehoben mit „Reset"-Button und blättert per „Prev/Next"-Buttons sowie `Alt+←` / `Alt+→` bzw. den Pfeiltasten zwischen den aktuell angezeigten Zeilen (in der aktiven Filter-/Sortierreihenfolge). `Esc` schließt den Dialog. Änderungen landen im gleichen Pending-Changes-Stack wie In-Place-Edits.
- **Single Record View — Layout-Stabilität:** Der Dialog hat jetzt einen festen Abstand von oben (40 px) und eine fixe Höhe (`100vh − 80px`), sodass er beim Wechsel zwischen Datensätzen unterschiedlicher Größe nicht mehr vertikal „springt". Beim Blättern mit Prev/Next (und nach Inline-Edits oder Reset) bleibt die Scrollposition im Dialog erhalten, statt jedes Mal zurück nach oben zu springen.
- **Neue / duplizierte Zeilen erscheinen direkt unter der ausgewählten Zeile** statt am Ende der Liste. Eine Zeile wird per Klick irgendwo in die Zeile ausgewählt (sichtbar durch einen Fokus-Streifen am linken Rand); „+ Insert Row" oder das Duplizieren-Symbol (⧉) hängen die neue Zeile direkt darunter an. Ist keine Zeile ausgewählt, wandert die neue Zeile an den Anfang der Tabelle. Wird die Anker-Zeile durch Filter/Sortierung ausgeblendet, fallen die zugehörigen neuen/duplizierten Zeilen auf den Tabellenende-Bereich zurück, damit sie nicht verloren gehen.

## 0.1.0

- Initial release
- PostgreSQL connection management
- Table explorer with schema browsing
- Table data viewer
- SQL editor with query execution
- Table search
